{
  "name": "selenium",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "ci-test": "jest --runInBand --ci --reporters=default --reporters=jest-junit",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "chromedriver": "^2.33.2",
    "jest": "^25.2.7",
    "jest-junit": "^10.0.0",
    "selenium-webdriver": "3.6.0"
  },
  "jest-junit": {
    "suiteName": "Integration Tests",
    "outputDirectory": ".",
    "outputName": "it-results.xml"
  }
}
