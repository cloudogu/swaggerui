{
  "name": "selenium",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "ci-test": "jest --runInBand --ci --testResultsProcessor='jest-junit'",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "chromedriver": "^80.0.2",
    "jest": "^25.2.7",
    "jest-junit": "^10.0.0",
    "selenium-webdriver": "3.6.0"
  },
  "jest-junit": {
    "suiteName": "Integration Tests",
    "output": "./it-results.xml"
  }
}
